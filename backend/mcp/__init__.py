"""
mcp — Model Context Protocol tool integrations.

Modules:
  arxiv_tool        — ArXiv paper search via REST API (no key required)
  github_tool       — GitHub repository search (60 req/hr free, 5000/hr with token)
  huggingface_tool  — HuggingFace datasets and models search
"""
